package main

// Difference relations in R, but not in S
// (R-S)
// SQL: (SELECT * FROM R) EXCEPT (SELECT * FROM S)
func Difference(relOne Relation, relTwo Relation) Relation {
	// result := make([]Row, 0)

	return Relation{}
}
