package main

// Union combines two relation sets: (R ∪ S).
// SQL: (SELECT * FROM R) UNION ALL (SELECT * FROM S)
func Union(relOne Relation, relTwo Relation) Relation {
	// result := make([]Row, 0)

	return Relation{}
}
